on_http_request:
  - name: Block everything except the Fathom webhook
    expressions:
      - "req.method != 'POST' || req.url.path != '/webhooks/fathom'"
    actions:
      - type: deny
        config:
          status_code: 404
